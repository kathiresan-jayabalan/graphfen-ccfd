# data/raw

This folder is intended for raw input datasets used by the GraphFEN-CCFD notebook.

- Place local `creditcard.csv` or equivalent dataset files here.
- Files in this folder are typically excluded via `.gitignore` to prevent large or private data from being pushed.